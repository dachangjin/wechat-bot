"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const wechaty_common_js_1 = require("./wechaty-common.js");
test("login", async () => {
    const bot = await (0, wechaty_common_js_1.prepareSingedOnBot)();
    expect(bot.isLoggedIn).toBeTruthy();
    await new Promise((resolve) => setTimeout(resolve, 2000));
    await bot.stop();
    expect(bot.isLoggedIn).toBeFalsy();
}, 300000); // 5 min
test("logout", async () => {
    const bot = await (0, wechaty_common_js_1.prepareSingedOnBot)();
    expect(bot.isLoggedIn).toBeTruthy();
    await new Promise((resolve) => setTimeout(resolve, 2000));
    await bot.logout();
    expect(bot.isLoggedIn).toBeFalsy();
}, 60 * 1000);
//# sourceMappingURL=wechaty-login.test.js.map