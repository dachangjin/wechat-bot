import { Room } from 'wechaty';
/**
 * string here should be the room id only.
 * topic should use the RegExp as the filter
 */
declare type RoomMatcherFunction = (room: Room) => boolean | Promise<boolean>;
declare type RoomMatcherOption = boolean | string | RegExp | RoomMatcherFunction;
export declare type RoomMatcherOptions = RoomMatcherOption | RoomMatcherOption[];
export declare function roomMatcher(matcherOptions?: RoomMatcherOptions): ((..._: any[]) => boolean) | ((room: Room) => Promise<boolean>);
export {};
//# sourceMappingURL=room-matcher.d.ts.map