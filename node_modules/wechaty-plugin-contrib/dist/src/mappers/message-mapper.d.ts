import { Contact, Message, FileBox, UrlLink, MiniProgram } from 'wechaty';
/**
 * 1. `undefined` means drop the message
 * 1. `Message` means forward the original message
 */
export declare type MappedMessage = undefined | Message | string | FileBox | Contact | UrlLink | MiniProgram;
declare type MessageMapperFunction = (message: Message) => MappedMessage | MappedMessage[] | Promise<MappedMessage | MappedMessage[]>;
declare type MessageMapperOption = MappedMessage | MessageMapperFunction;
export declare type MessageMapperOptions = MessageMapperOption | MessageMapperOption[];
declare function messageMapper(mapperOptions: MessageMapperOptions): (message: Message) => Promise<MappedMessage[]>;
export { messageMapper, };
//# sourceMappingURL=message-mapper.d.ts.map