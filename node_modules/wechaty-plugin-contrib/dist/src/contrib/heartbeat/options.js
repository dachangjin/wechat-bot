"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.buildConfig = void 0;
const DEFAULT_CONTACT_ID = 'filehelper';
const DEFAULT_INTERVAL_SECONDS = 60 * 60; // 1 Hour
/**
 *   emoji: {
 *     heartbeat : '[爱心]',
 *     login     : '[太阳]',
 *     logout    : '[月亮]',
 *     ready     : '[拳头]',
 *   },
 */
const DEFAULT_HEARTBEAT_CONFIG = {
    emoji: {
        heartbeat: '[爱心]',
    },
    intervalSeconds: DEFAULT_INTERVAL_SECONDS,
};
function buildConfig(config) {
    const normalizedConfig = Object.assign(Object.assign(Object.assign({}, DEFAULT_HEARTBEAT_CONFIG), config), { emoji: Object.assign(Object.assign({}, DEFAULT_HEARTBEAT_CONFIG.emoji), config === null || config === void 0 ? void 0 : config.emoji) });
    /**
     * Set contact to DEFAULT_CONTACT_ID if there's nothing set
     */
    if (!normalizedConfig.room && !normalizedConfig.contact) {
        normalizedConfig.contact = DEFAULT_CONTACT_ID;
    }
    return normalizedConfig;
}
exports.buildConfig = buildConfig;
//# sourceMappingURL=options.js.map