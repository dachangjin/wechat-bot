#!/usr/bin/env ts-node
"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const tstest_1 = __importDefault(require("tstest"));
const sinon_1 = __importDefault(require("sinon"));
const message_talker_1 = require("./message-talker");
tstest_1.default('messageTalker()', (t) => __awaiter(void 0, void 0, void 0, function* () {
    const spy2 = sinon_1.default.spy();
    const spy3 = sinon_1.default.spy();
    const spy4 = sinon_1.default.spy();
    const EXPECTED_TEXT = 'text';
    const OPTIONS_TEXT = EXPECTED_TEXT;
    const OPTIONS_FUNCTION_LIST = [spy2, spy3];
    const mockMessage = {
        say: spy4,
        wechaty: {
            sleep: () => undefined,
        },
    };
    let talkMessage = message_talker_1.messageTalker(OPTIONS_TEXT);
    spy4.resetHistory();
    yield talkMessage(mockMessage);
    t.true(spy4.called, 'should called the contact.say');
    t.equal(spy4.args[0][0], EXPECTED_TEXT, 'should say the expected text');
    talkMessage = message_talker_1.messageTalker(OPTIONS_FUNCTION_LIST);
    spy2.resetHistory();
    spy3.resetHistory();
    yield talkMessage(mockMessage);
    t.true(spy2.called, 'should called the functions 1');
    t.equal(spy2.args[0][0], mockMessage, 'should called the functions 1/1 with mockMessage');
    t.true(spy3.called, 'should called the functions 2');
    t.equal(spy3.args[0][0], mockMessage, 'should called the functions 2/1 with contact');
}));
tstest_1.default('messageTalker() with mustache', (t) => __awaiter(void 0, void 0, void 0, function* () {
    const EXPECTED_TEXT = 'Hello, world!';
    const OPTIONS_TEXT = 'Hello, {{ name }}!';
    const VAR = 'world';
    const spy = sinon_1.default.spy();
    const mockMessage = {
        say: spy,
        wechaty: {
            sleep: () => undefined,
        },
    };
    const view = { name: VAR };
    const talkMessage = message_talker_1.messageTalker(OPTIONS_TEXT);
    yield talkMessage(mockMessage, view);
    t.true(spy.called, 'should called the contact.say');
    t.equal(spy.args[0][0], EXPECTED_TEXT, 'should say the expected text');
}));
//# sourceMappingURL=message-talker.spec.js.map