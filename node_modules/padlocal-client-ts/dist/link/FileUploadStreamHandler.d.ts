import { StreamHandler } from "./StreamHandler";
import { WeChatStreamRequest } from "../proto/padlocal_pb";
import { Bytes } from "../utils/ByteUtils";
import { Request } from "../Request";
export declare type FileUploadStreamHandlerParams = {
    aesKey: Bytes;
    dataBag: {
        [key: string]: Bytes;
    };
};
export declare class FileUploadStreamHandler extends StreamHandler {
    private _socketClient?;
    private readonly _params;
    constructor(request: Request, params: FileUploadStreamHandlerParams);
    onRequest(weChatStreamRequest: WeChatStreamRequest): void;
    private _assembleUploadPayload;
}
