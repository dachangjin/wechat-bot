export declare class RetryStrategy {
    private static readonly FAST_RETRY_DELAYS;
    readonly maxRetry: number;
    private _retryCount;
    private readonly _retryDelays;
    private constructor();
    get retryCount(): number;
    static getStrategy(rule: RetryStrategyRule, maxRetry: number): RetryStrategy;
    canRetry(): boolean;
    reset(): void;
    /**
     * @return millisecond
     */
    nextRetryDelay(): number;
}
export declare enum RetryStrategyRule {
    FAST = 0
}
